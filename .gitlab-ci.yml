cache:
  paths:
  - mushrooms/node_modules/
  - books/node_modules/
  - ui/node_modules/

stages:
  - compile
  - dockerize

mushrooms:
  stage: compile
  image: node:11
  script:
   - cd mushrooms
   - npm install
   - npm run build
books:
  stage: compile
  image: node:11
  script:
    - cd books
    - npm install
    - npm run build

ui:
  stage: compile
  image: node:11
  script:
    - cd ui
    - npm install
    - npm run build

dockerize:
  stage: dockerize
  script:
    - docker build -t piotrjanik/opa-demo-mushrooms mushrooms
    - docker build -t piotrjanik/opa-demo-books books
    - docker build -t piotrjanik/opa-demo-ui ui
    - docker push piotrjanik/opa-demo-mushrooms
    - docker push piotrjanik/opa-demo-books
    - docker push piotrjanik/opa-demo-ui